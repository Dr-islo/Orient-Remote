package com.orientremote.app.util

object Constants {
    /** Repeat interval while a repeatable button (volume/channel) is held down. */
    const val LONG_PRESS_REPEAT_INTERVAL_MS = 250L

    /** Buttons become "repeatable" (hold-to-repeat) after this initial delay. */
    const val LONG_PRESS_INITIAL_DELAY_MS = 350L
}
